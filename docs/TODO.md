# TODO — design-system

_Sections are stable areas. Items carry their added date inline. `[!]` = urgent. Completed items go to [TODO-archive.md](TODO-archive.md)._

---

## Backlog

- [ ] **Pilot: adopt in `budget/web`** — already has `components.json`; point its `registries` field at this repo and pull `theme` + `button` to validate the flow end-to-end (added 2026-07-12)
- [ ] **Migrate `myargus/packages/ui`** — convert the hand-rolled package to consume this registry instead of maintaining components independently (added 2026-07-12)
- [ ] **Adopt in `atto1`** — no existing component library, cleanest first real consumer (added 2026-07-12)
- [ ] **Tag a v0.1.0 release** once 3-4 components are stable, so consumers can pin instead of tracking `main` (added 2026-07-12)
